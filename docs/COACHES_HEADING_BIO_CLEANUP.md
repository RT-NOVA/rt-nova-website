# Coaches Heading + Bio Cleanup

This patch keeps the current staff row/table flow but cleans up the section headings and head coach bio action.

## Changes

- Keeps the kicker / heading / note pattern.
- Centers section headings on tablet/desktop to align with the centered page content.
- Keeps heading sizes below hero scale.
- Changes the head coach bio control from a boxed badge to a subtle inline text action:
  - `Bio +`
  - `Bio −` when open
- Keeps the staff row structure unchanged.
- Keeps click-to-preview coach images.
