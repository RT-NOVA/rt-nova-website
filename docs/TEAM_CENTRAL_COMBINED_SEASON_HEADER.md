# Team Central combined selector / season header

This patch visually combines the white season selector area and the dark selected-season summary into one connected dashboard header.

It does not change team data or season sorting.

Apply with:

```bash
> Historical note: the one-time migration script referenced here was removed after its changes were applied. The current source and Git history are authoritative.
```

Then run:

```bash
hugo server -D
```
